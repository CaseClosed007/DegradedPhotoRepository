# ── Python ────────────────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# ── Machine Learning ──────────────────────────────────────────────────────────
# raw training data (too large for git)
data/
# large zip archives
*.zip
# model weights are committed directly (38MB — under GitHub's 100MB limit)

# ── App output ────────────────────────────────────────────────────────────────
# user-generated enhanced photos
enhanced_output/
# runtime scan manifest (regenerated on every scan)
scan_results.json

# ── Electron build output ─────────────────────────────────────────────────────
# built .dmg / .exe installers
dist/
# root Electron node_modules
node_modules/

# ── React / Node ──────────────────────────────────────────────────────────────
frontend/node_modules/
# built frontend (regenerated by npm run build)
frontend/dist/

# ── Presentation & reports ────────────────────────────────────────────────────
DegradedPhotoDetection.pptx
generate_ppt.py
report.html
report.pdf

# ── GoPro raw dataset ─────────────────────────────────────────────────────────
Gopro/
photo_collection/

# ── Environment & secrets ─────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── OS & editor ───────────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp
*.swo
